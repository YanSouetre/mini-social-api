{
  "name": "mini-social-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --routes routes.json --delay 250"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
